---
type: Security
title: The dedicated server's licence escape folds shut in shipped builds
---

The config-file switch that admits players over a raw UDP socket (the Steam-only licence gate's testing escape) is now dead code in the packaged desktop build — a build-time literal folds it shut, so editing the config can no longer open unlicensed hosting on a shipped binary.
