---
type: Changed
title: One desktop app, and a smaller one
---

The desktop download is now built on each system's own web view rather than carrying a browser of its own, so it is a fraction of the size. There is one build per platform — an installer for Windows, a disk image for macOS, an AppImage and a .deb for Linux — where there used to be two. Heroes carry across through Steam Cloud.
