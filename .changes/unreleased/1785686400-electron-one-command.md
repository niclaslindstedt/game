---
type: Fixed
title: One-command Electron development
---

Running `npm run electron` from the repository root now installs the desktop dependencies, bundles the game, and launches the Electron app without a preload crash.
