---
type: Fixed
title: HUD clears the iOS notch
---

The top HUD (XP strip, hero portrait unit, and minimap) now clears the iOS notch / Dynamic Island by honoring the top safe-area inset, instead of hiding behind it; displays without an inset keep the XP strip flush to the top edge.
