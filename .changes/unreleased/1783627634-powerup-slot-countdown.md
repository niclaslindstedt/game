---
type: Changed
title: Powerups count down in their dock slot
---

An activated powerup now holds its dock slot and counts down in place with its stopwatch overlay instead of jumping to a separate strip; the slot only frees and the rest shift down once the power lapses, so the dock stays full — and no new powerup can be picked up — while a power runs.
