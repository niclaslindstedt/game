---
type: Fixed
title: Settings pages that are only doors
---

A settings page whose rows are all destinations — DATA, GALLERIES, DEVELOPER —
now centres its column instead of stranding it against the left, and its help
line stays quiet until a row is actually highlighted.
