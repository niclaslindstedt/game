---
type: Fixed
title: Post-update resume freeze
---

Resuming a run parked before an app update no longer freezes the game on a
still image with no UI: a snapshot the new build can't read is dropped (CONTINUE
simply doesn't appear), and a run that fails on its very first frame now shows
the reload screen instead of hanging.
