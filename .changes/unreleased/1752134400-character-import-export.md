---
type: Added
title: Import and export characters
---

**SETTINGS → EXPORT / IMPORT CHARACTER** carry a hero between devices: export the active character to a small signed `.zip` (an HMAC-SHA256 signature over the save makes casual tampering fail), and import one back through a file picker — bringing its level, gear, and campaign progress across.
