---
type: Changed
title: Main-menu version in the pixel font
---

The version and build label in the main-menu corner now renders in the in-game pixel font (a new middle-dot glyph was added to the font atlas) so it matches the rest of the menu instead of the browser's default font.
