---
type: Changed
title: Spawn points hold steady pressure instead of dumping a pile
---

Each spawn point now caps how many of its own members stand at once (`maxAlive`, ~15 on every map) and only emits while the hero is in trigger range — it drips a replacement per kill for steady local pressure and pauses when he walks away, instead of streaming its whole queue onto the field at once.
