---
type: Fixed
title: Typing in JOIN BY ADDRESS
---

The JOIN BY ADDRESS box (and the session password, port and hero-name fields)
now takes the keyboard the moment it opens instead of ignoring every keystroke
when it was opened with a click.
