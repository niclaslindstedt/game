---
type: Changed
title: Sprite-styled update prompt
---

The "a new version is ready" prompt is now a sprite-based panel — pixel font, the upgrade sprite, and chunky pixel buttons — so it matches the game instead of the framework's plain system-font toast.
