---
type: Fixed
title: Desktop downloads for every platform
---

A release attaches a build for Windows, Linux and both macOS architectures again — the Windows and Linux ones failed to package and never reached the release page.
