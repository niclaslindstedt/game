---
type: Fixed
title: Compact update prompt in landscape
---

The "a new version is ready" prompt now keeps its buttons on one row to the right of the text in landscape instead of wrapping them underneath, so the panel no longer grows taller than it needs to when there is horizontal room to spare.
