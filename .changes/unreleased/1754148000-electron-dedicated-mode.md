---
type: Added
title: Headless desktop server mode
---

Run the desktop executable with `--dedicated`, optional `--bots 1` through `--bots 8`, and `--verbose` status output to test multiplayer against a headless server; eight bots start an autonomous run immediately, lifecycle events are logged, and Ctrl-C gives players a one-minute shutdown countdown.
