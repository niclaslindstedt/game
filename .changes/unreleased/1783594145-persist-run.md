---
type: Fixed
title: CONTINUE survives app updates
---

Your in-progress run is now saved to the browser so exiting to the menu and then applying an app update (which reloads the page) keeps the CONTINUE button instead of dropping the run.
