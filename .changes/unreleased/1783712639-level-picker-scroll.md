---
type: Fixed
title: Level picker no longer scrolls when it fits
---

The developer SELECT LEVEL / mission list only scrolls when the ladder is too tall for the viewport, so the short shipped list no longer shows a needless scrollbar or clips its top row.
