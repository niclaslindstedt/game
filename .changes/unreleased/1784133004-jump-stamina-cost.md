---
type: Changed
title: Jumping costs stamina
---

Each jump now spends a slice of the sprint pool (10% of max stamina per takeoff), and bottoming the pool out with a run or a jump freezes stamina regen for two seconds — so the hero can't tap-run or tap-jump on fumes and must walk it off to recover.
