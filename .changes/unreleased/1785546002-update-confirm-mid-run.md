---
type: Added
title: Update confirm mid-run
---

Applying an app update while a run is parked now asks for confirmation first —
a parked run only survives the reload when the new build still reads the old
save format, so UPDATE warns instead of quietly gambling the run.
