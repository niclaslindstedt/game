---
type: Fixed
title: SPIRIT no longer reads UNDEFINED on legacy heroes
---

A character created before the SPIRIT stat shipped now backfills it (and every other stat) to 0 on load, so the level-up chooser shows "SPIRIT 0" instead of "SPIRIT UNDEFINED".
