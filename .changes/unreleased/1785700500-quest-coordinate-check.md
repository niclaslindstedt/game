---
type: Fixed
title: A quest spot off the edge of its own map is now a build error
---

An errand's coordinates — a giver's post, a place to find, an escort's destination, a piece left on the floor — are now checked against the real size of the map they name, so a spot past the edge fails the build instead of shipping as an objective nothing can ever reach.
