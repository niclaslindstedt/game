---
type: Fixed
title: The desktop game says why it cannot start
---

Launching the desktop game where there is no desktop — over SSH, in a container, on a machine whose session is not running — now explains that in one line instead of printing a page of internal error text.
