---
type: Added
title: Mods can ship drawn sprites
---

A mod can now drop a `.png` into `sprites/` in place of a pixel grid — the file name is the sprite it replaces, so a folder of drawn art is a complete mod on its own.
