---
type: Added
title: Powerup dock reordering, and running slots can be dropped
---

The powerup dock supports reordering (a `moveItem` input; running powers travel with their countdown), and a slot whose power is already running can now be dropped — the effect runs out its clock while the slot frees for new loot immediately.
