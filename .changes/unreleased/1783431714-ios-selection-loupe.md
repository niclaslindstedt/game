---
type: Fixed
title: No selection loupe on double-tap
---

Double-tapping the game on iOS (routine, since a tap jumps) no longer pops the text-selection magnifier/loupe over the screen; the canvas now suppresses the touch selection gesture while pointer steering keeps working.
