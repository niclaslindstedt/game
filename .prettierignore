# Vendored / generated — not ours to format
OSS_SPEC.md
CHANGELOG.md
LICENSE
package-lock.json
pwa/dist/
pwa/src/generated/
pwa/src/game/assets/
engine/generated/
native/ios/
native/android/
# Desktop shell build artifacts: the copied website, the compiled main process,
# and electron-builder's installers. All regenerated, none ours to format.
electron/webroot/
electron/dist/
electron/release/
# The Tauri shell's copied website and its packaged depot. Its Rust is formatted
# by `cargo fmt`, which Prettier has nothing to say about, and its placeholder
# page is a build stub.
tauri/webroot/
tauri/release/
tauri/resources/
tauri/src-tauri/placeholder/
site/
node_modules/

# Agent skill playbooks are templated upstream; keep their formatting stable
.agent/
.agents/
.codex/

# The mod reference catalog is generated AND byte-compared by its own drift test
# (tests/content/mod_catalog_test.ts). Two tools that both want to write a file
# whose exact bytes are the assertion is a trap: `make fmt` would collapse its
# short arrays, the drift check would then fail, and regenerating would undo the
# formatting. Its generator owns it.
mod/catalog.json
